# PreBIM-SteelStructure — Worklog (2026-02-09)

This log summarizes changes made during the session.

## Naming / product
- Renamed UI branding to **PreBIM-SteelStructure** across landing + app.
- Removed AI/dev-centric marketing copy from landing and app footer.

## Core editor / UX
- Realtime apply fixes for Levels add/delete.
- Grid UI simplified to spans-only.
- Horizontal members align top to level line.
- 3D profile rendering upgraded: H/I/C/LC/T/L approximations via merged primitives.
- Bracing upgraded:
  - Panel pick enabled when popup open; disabled when closed.
  - Brace types: X, single diagonal (/), chevron (ㅅ).
  - Brace profile snapshotted per picked panel.
- Joists disabled (feature unused; removed stray lines).

## 3D visibility
- Lighting/tonemapping tuned; outlines always on.
- Braces rendered as solids.
- Added grid/level guides in 3D:
  - Base grid lines + X/Y labels offset outside model.
  - Level outlines + labels offset outside model.
  - Guides toggle button.
  - Label size tuning (last build: 20260209-0720).

## Quantities
- Quantities panel moved to bottom full-width, collapsible by default.
- Copy-to-Excel (TSV) support.
- Total weight shown.
- Quantities now split by **profile** for column/beam/subbeam/brace (and joist if enabled).
- Stable category ordering + per-category subtotals.

## Exports
- Added topbar buttons: DATA, STAAD, DXF, IFC placeholder.
- DXF auto-dimension export (grid spans + levels) and later enhancements.
- DXF updated to use profile-based widths and column section outlines (approx).

## Documentation
- In-app Manual button + /prebim/manual.html.
- Published /prebim/MANUAL.md and generated PDF /prebim/PreBIM-SteelStructure-Manual.pdf.
- Added /prebim/version.json to show build on manual page.
- Fixed PDF 403 by chmod 644.

## Reliability / caching
- Added build stamp in UI and cache-buster guidance (?v=... before #).

## Recent deployed build
- Latest deployed tag observed: deploy-20260209T065940Z
- Current HEAD: fb3ac3d (deploy log commit)

## Notes
- memory_search tool unavailable (no API key). This log is the durable record.
